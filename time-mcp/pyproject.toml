[project]
name = "time-mcp"
version = "0.1.0"
description = "MCP server for time, timezone, and date conversions"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.2.0",
    "tzdata>=2024.1",
    "python-dateutil>=2.9.0",
]
